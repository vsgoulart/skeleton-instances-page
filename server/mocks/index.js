const {WORKFLOWS} = require('./workflows');
const {STATISTICS} = require('./statistics');
const {INSTANCES} = require('./instances');
const {OPERATIONS} = require('./operations');

module.exports = {WORKFLOWS, STATISTICS, INSTANCES, OPERATIONS};
